
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Foca no Blog -> {{title}}</title>
    <meta name="description" content="{{description}}" />
    <meta name="author" content="Fabio F.S." />
    <meta name="robots" content="index, follow" />
    <link rel="shortcut icon" type="image/x-icon" href="favicon.ico">
    <!-- Link para a fonte Inter do Google Fonts para um visual moderno -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <!-- Link para o arquivo CSS de estilos -->
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/public/css/style.css">
    <style>
      /* Estilos para o Tailwind que dependem de variáveis do CSS customizado */
      .text-blue-600 { color: var(--color-blue); }
      .text-red-500 { color: var(--color-red); }
      .hover\\:text-red-500:hover { color: var(--color-red); }
    </style>
    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-WDEC7PMX5R"></script>
    <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());

    gtag('config', 'G-WDEC7PMX5R');
    </script>
</head>
<body class="bg-white text-gray-800 antialiased">

    {% include "header.html" %}

    <div class="container mx-auto px-4 md:px-0 lg:grid lg:grid-cols-4 lg:gap-12">

        <!-- Coluna Central com Conteúdo Principal -->
        <main class="lg:col-span-3">
          <div class="pt-[35px]"></div>
          <section class="artigo container mx-auto px-4 md:px-8 mt-2 mb-20">
            <h1 class="text-3xl font-bold text-gray-900">{{title}}&nbsp;<span class="data-artigo"> (criado em {{ date | formatBR }})</span></h1>
            {{ content | safe }}
           </section>
        </main>

        <!-- Coluna Direita para Anúncios (visível apenas em telas grandes) -->
        <aside class="hidden lg:block">
            <div class="pt-[60px]"></div>
            <div class="p-4 bg-gray-100 rounded-lg text-center mb-4">
                <h4 class="font-bold mb-2">Dicas de Livros</h4>
                <div id ="anuncios_lateral">
                </div>
            </div>    
        </aside>

    </div>


    <section class="container mx-auto px-4 md:px-8 mt-10 mb-10">
        <div id="anuncios_rodape" class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
        </div>
    </section>

    {% include "footer.html" %}

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const menuButton = document.getElementById('menu-button');
            const mobileMenu = document.getElementById('mobile-menu');

            menuButton.addEventListener('click', () => {
                mobileMenu.classList.toggle('hidden');
            });
        });

        //const dataOriginal = "{{date}}";
        //const data = parse(dataOriginal, "yyyy-MM-dd", new Date());
        //const dataFormatada = format(data, "dd/MM/yyyy");
        //document.getElementById("data").innerText = ` (criado em ${dataFormatada})`;
    </script>
    <script defer src="/public/js/anuncios.js"></script>
</body>
</html>